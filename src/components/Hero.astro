<section class="hero">
  <div class="red-line"></div>
  <div class="hero-content">
    <h1>PREMIUM MOTORCYCLE EXPERIENCE</h1>
  </div>
</section>

<style>
  .hero {
    background-color: #000;
    min-height: 100vh;
    padding-top: var(--header-height);
    position: relative;
  }

  .red-line {
    position: absolute;
    top: var(--header-height);
    left: 0;
    right: 0;
    height: 40px;
    background-color: #ff0000;
  }

  .hero-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 4rem 2rem;
    color: #fff;
  }

  h1 {
    font-size: 2rem;
    font-weight: 500;
    letter-spacing: 2px;
    color: #ffd700;
    margin-top: 40px;
  }

  @media (max-width: 768px) {
    .red-line {
      height: 30px;
    }

    h1 {
      font-size: 1.5rem;
    }
  }
</style> 